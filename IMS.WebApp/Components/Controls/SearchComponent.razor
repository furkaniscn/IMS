@if (searchFilter == null)
{
    searchFilter = string.Empty;
}
@* Enchance ekledik çünkü arama yaparken httpost olarak davranıp sayfayı yeniliyordu(document format)
    bunu değişitrdik(fetch format), şu an partial olarak çalışıyor.*@
<EditForm Enhance="true" Model="searchFilter" FormName="formSearch" OnSubmit="Search">
    <div class="input-group">
        <InputText class="form-control"
                   placeholder="Type something here to search"
                   @bind-Value="this.searchFilter">
        </InputText>
        <button type="submit" class="btn-success input-group-text">Search</button>
    </div>
</EditForm>
